import { ScoreCardNamePipe } from './score-card-name.pipe';

describe('ScoreCardNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ScoreCardNamePipe();
    expect(pipe).toBeTruthy();
  });
});
